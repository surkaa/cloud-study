server:
  port: 8081

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/db_cloud_order_server
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  global-config:
    db-config:
      # 逻辑删除配置
      logic-delete-field: flag
      logic-delete-value: 1
      logic-not-delete-value: 0