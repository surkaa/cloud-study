spring:
  application:
    name: user-server
  profiles:
    active: dev
  cloud:
    nacos:
      # nacos地址
      server-addr: localhost:8848
      config:
        # 配置文件的后缀
        file-extension: yaml